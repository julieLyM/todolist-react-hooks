import React,{useState} from 'react';

const TodoForm = () => {
    const [state, setstate] = useState(initialState)
  return (
    <div>
      <form>
        <input />
        <button>click</button>
      </form>
    </div>
  );
};

export default TodoForm;
