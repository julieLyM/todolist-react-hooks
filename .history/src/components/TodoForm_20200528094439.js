import React,{useState} from 'react';

const TodoForm = () => {
    const [, set] = useState(initialState)
  return (
    <div>
      <form>
        <input />
        <button>click</button>
      </form>
    </div>
  );
};

export default TodoForm;
